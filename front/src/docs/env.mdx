import Table from "./Table";

# My Storybook Documentation

<Table
  headers={["Variable", "Description"]}
  rows={[
    [
      "MYSQL_ROOT_PASSWORD",
      "This variable is used to specify the root password for the MySQL database server. It grants full access to all databases and actions on them.",
    ],
    [
      "MYSQL_DATABASE",
      "This variable specifies the name of the database that will be created when the MySQL Docker container is started. In this case, it indicates the name of the database, which could be used for a WordPress installation.",
    ],
    [
      "MYSQL_USER",
      "This variable sets the username for accessing the MySQL database. Replace with your desired username",
    ],
    [
      "MYSQL_PASSWORD",
      "This variable is used to define the password associated with the specified MYSQL_USER Replace with the desired strong password for the MySQL user",
    ],
  ]}
/>
