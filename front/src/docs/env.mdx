
import Table from "./Table";

# Environment Variables

The .env file is located in the root directory of the project

<Table
  headers={["Variable", "Description"]}
  rows={[
    [
      "MYSQL_ROOT_PASSWORD",
      "This variable is used to specify the root password for the MySQL database.",
    ],
    [
      "MYSQL_DATABASE",
      "This variable specifies the name of the database that will be used in our application. This is the wordpress database",
    ],
    [
      "MYSQL_USER",
      "This variable is the username for the Wordpress MySQL database.",
    ],
    [
      "MYSQL_PASSWORD",
      "This variable is the password for the Wordpress MySQL database.",
    ],
    [
      "VITE_IMAGE_PATH",
      "URL path to images saved in wp-content/uploads. currently a url on the localhost:8000",
    ],
     [
      "VITE_SERVER_BASE_URL",
      "Environment variable pointing to the backend server location",
    ],
  ]}
/>

